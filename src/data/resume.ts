import type { Resume } from "./types";

/* =========================================================================
   EDIT ME — resume.
   1. Drop your PDF into the /public folder (e.g. /public/resume.pdf).
   2. Point `file` at it. Anything in /public is served from "/".
   ========================================================================= */

export const resume: Resume = {
  file: "/resume.pdf",
  downloadName: "John_Ortega_Resume.pdf",
  updated: "June 2026",
};
